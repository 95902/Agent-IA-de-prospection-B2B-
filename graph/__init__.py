"""Graphe LangChain — orchestration des nodes (issue #28).

Issue #11 — STUBS. `workflow.py` assemble les 6 nodes, `state.py` définit
l'état partagé (TypedDict) du graphe. L'implémentation réelle est portée par
#28/#29. Aucun ICP codé en dur (règle #3).
"""