# Dépendances socle (issues #8 embeddings + #9 db). Python 3.12.
# Async partout (règle #8) : asyncpg, AsyncQdrantClient, httpx.
asyncpg>=0.29,<0.31
# Aligné sur la version du serveur Qdrant du docker-compose (v1.15.x, issue #6).
# Le client doit suivre le serveur (écart de version mineure toléré : 1 max).
qdrant-client>=1.15,<1.16
httpx>=0.27,<0.29
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0


# Validation des modèles (issue #10)
phonenumbers>=8.13            # normalisation téléphone -> E.164
email-validator>=2.1          # validation email RFC (utilisé par le validator, pas EmailStr)
python-stdnum>=1.19           # SIRET/SIREN : checksum Luhn + exception La Poste

# Enrichissement (issue #18) — cascade Tavily (via httpx) + DuckDuckGo
beautifulsoup4>=4.12          # extraction mailto/emails du HTML
ddgs>=6.0                     # DuckDuckGo (fallback recherche, sans clé)
# crawl4ai : volontairement ABSENT — voir requirements-optional.txt

# Scoring LLM (issue #25) — API Claude UNIQUEMENT (règle #6), AsyncAnthropic (règle #8).
# Sortie structurée via output_config.format (json_schema) — nécessite un SDK récent.
# #25 figera la version exacte quand il consomme réellement l'API ; pip résout au dernier.
anthropic>=0.40,<1.0
# Rendu des prompts de scoring depuis l'ICP client (issue #25), jamais figé (règle #3).
jinja2>=3.1,<4.0

# --- Dev (tests) ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.25

