"""Smoke test — vérifie que la stack (Postgres, Qdrant, Ollama) répond.

Issue #11 — STUB. Implémentation détaillée portée par une issue dédiée
(#34). Lancé via `python scripts/smoke_test.py` (cf. CLAUDE.md).
"""
from __future__ import annotations


def main() -> int:
    """STUB — vérifie la disponibilité des services Docker. À implémenter."""
    raise NotImplementedError("scripts/smoke_test non implémenté — voir issue dédiée.")


if __name__ == "__main__":
    raise SystemExit(main())